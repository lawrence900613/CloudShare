VITE_API_BASE_URL=http://localhost:8080
# Must match APP_FILES_MAX_PER_USER in application.yml (default: 3)
VITE_MAX_FILES_PER_ACCOUNT=3
# Storage quota shown in the UI per user (MB)
VITE_STORAGE_CAPACITY_MB=100
